package Tokens;

public class Variablse {
    public static void main ( String[] args ) {
        int a = 10; // This is valid variable name
        //int 1a = 20;  // not a valid variable name
        String str1 = "Ravi"; // This is valid because number can come after declaration
        float _floatNum = 6;   // This is also valid because variable can start with _ .

        byte nu$1 = 20; // this is also valid because $ is allowed
        int $num = 12;  // this is also valid


    }
}
